# Скопируйте этот файл как deploy/.env и вставьте пароль.
# deploy/.env в репозиторий не попадает (.gitignore).

SSH_PASS=

# ava.nmiczd.ru (опционально; иначе см. scripts/ssh_remote.py)
# AVA_SSH_HOST=178.170.165.72
# AVA_SSH_USER=user_adm
# AVA_APP_DIR=/var/www/ava.nmiczd.ru
# SUDO_PASS=   # если отличается от SSH_PASS
# CERTBOT_EMAIL=webmaster@nmiczd.ru
